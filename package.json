{
  "name": "docker-express-pg",
  "version": "0.0.1",
  "description": "A docker powered app that utilize express and postgres with redis store",
  "main": "dist/src/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "https://github.com/LitHaxor/Docker-express.git",
  "author": "HASAN AHAMED",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.1",
    "express": "^4.17.1",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  }
}
